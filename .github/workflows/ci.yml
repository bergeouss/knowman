name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Get Bun cache directory
        id: bun-cache
        run: |
          echo "dir=$(bun pm cache)" >> $GITHUB_OUTPUT

      - name: Cache Bun dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.bun-cache.outputs.dir }}
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint all packages
        run: bun run lint

      - name: Type check all packages
        run: bun run type-check

      - name: Test all packages
        run: bun run test
        env:
          NODE_ENV: test
          DATABASE_URL: sqlite:./test.db
          REDIS_URL: redis://localhost:6379
          JWT_SECRET: test-secret
          LOG_LEVEL: error

      - name: Build all packages
        run: bun run build

  # Separate job for building extension (might need additional setup)
  build-extension:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build extension
        run: bun run build:extension

      - name: Package extension
        run: |
          cd packages/extension
          zip -r extension.zip dist/
        continue-on-error: false

      - name: Upload extension artifact
        uses: actions/upload-artifact@v4
        with:
          name: browser-extension
          path: packages/extension/extension.zip

  # Optional: Deploy preview (could be added later)
  # deploy-preview:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   if: github.event_name == 'pull_request'
  #   steps:
  #     - run: echo "Deploy preview would go here"